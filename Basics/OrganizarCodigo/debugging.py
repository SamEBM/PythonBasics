import pdb

def suma(num1, num2):
    pdb.set_trace()
    return num1 + num2

suma(4, 'sashasua')

# (Pdb) num1
# (Pdb) num2
# (Pdb) num2 = 3
# (Pdb) list -> Las lineas de codigo
# (Pdb) step -> Imprime el error del return
# (Pdb) a -> Imprime los argumentos de la funcion actual
# (Pdb) w -> Detalla donde estamos